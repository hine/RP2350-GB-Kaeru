# Waveshare RP2350-Touch-AMOLED-1.8 物理ボタン調査結果

調査日: 2026-06-03

---

## 概要

基板上には以下の2つの物理ボタンがある。

| ボタン | 回路上の名称 | 主な接続先 | プログラムからの扱いやすさ | 主な用途 |
|--------|------------|-----------|--------------------------|---------|
| BOOT | Key2 | QSPI_SS_N / QSPI_SS | 通常 GPIO としては扱いにくい | BOOTSEL / 書き込みモード |
| POWER / PWR | Key1 | PWRON + GPIO18 | GPIO18 として読み取り可能 | 電源制御 + ユーザー入力 |

**結論:** アプリケーション用のカスタムボタンとして使えるのは実質 `POWER ボタン = GPIO18` のみ。

---

## BOOT ボタン

### 回路上の接続

回路図上では `Key2` と記載。RP2350A の通常 GPIO ではなく外部 Flash 関連信号の
`QSPI_SS_N / QSPI_SS` に接続されている。Raspberry Pi Pico 系の `BOOTSEL` ボタンに相当。

### 通常 GPIO として読み取れるか

読み取れない。`QSPI_SS_N` は外部 Flash のチップセレクト信号であり、通常動作中は
Flash アクセスに使われる。任意のアプリケーションボタンとして常時読み取る用途には適さない。

### 特殊 API による読み取り可能性

C/C++ SDK や MicroPython の一部環境では BOOTSEL ボタン専用の読み取り API が存在するが、
この API は Flash アクセスを一時停止して QSPI_SS_N の状態をサンプリングする仕組みのため、
**エミュレーション実行中（Flash XIP 使用中）に呼ぶと XIP が無効化されハードフォルトする危険がある**。

### 実用上の位置づけ

- 書き込みモードへの移行（BOOTSEL）
- 起動時のモード切り替え
- デバッグ・メンテナンス用途

通常のゲーム操作ボタンとして使用するのは**不可**。

---

## POWER / PWR ボタン

### 回路上の接続

回路図上では `Key1` と記載。以下の2系統に関係している。

- AXP2101（電源管理 IC）の `PWRON` 系
- RP2350A の `GPIO18`

### GPIO18 として読み取る方法

```c
// 初期化
gpio_init(18);
gpio_set_dir(18, GPIO_IN);
gpio_pull_up(18);

// 読み取り: 0 = 押下（active-low）
if (!gpio_get(18)) {
    // ボタンが押されている
}
```

| 状態 | GPIO18 の値 |
|------|-----------|
| 押していない | 1 |
| 押している | 0 |

### USB 給電中の長押しの挙動（実機確認）

USB 給電中に POWER ボタンを長押ししても電源が落ちなかった。

**理由:** AXP2101 は `VBUS`（USB 電源）が存在すると外部電源があると判断し、
長押しによる電源 OFF 処理を無効化する（または即時復帰させる）仕様と考えられる。

→ **USB 給電中は短押し・長押しを問わず GPIO18 入力として安全に使える**。

### バッテリー使用時の注意

バッテリー単体動作時は `VBUS` が存在しないため、AXP2101 が長押しを電源 OFF イベントとして処理する可能性がある。

| 動作状況 | POWER 長押し時の想定 |
|---------|-------------------|
| USB 給電のみ | 何も起きない（実機確認済み） |
| USB + バッテリー | USB 給電が優先され、見かけ上 OFF にならない可能性 |
| バッテリー単体 | AXP2101 が電源出力を落とし、システム OFF になる可能性 |

バッテリー使用時にゲーム操作ボタンとして使う場合は、押下時間を計測して
閾値（例: 2 秒）超えで入力を無視するロジックが必要。

### ゲームへの割り当て

本プロジェクトでは以下の理由で POWER ボタン = A ボタンとして割り当てている。

- FT3168 タッチコントローラが **1点タッチのみ対応**
- 方向キー（タッチ左半分）と A ボタンの同時入力がタッチ単独では不可能
- 「カエルの為に鐘は鳴る」のジャンプ・攻撃は方向 + A の同時押しを要する

**現在の制限:** USB 給電での動作のみ検証済み。バッテリー使用時は未検証。

---

## 推奨する使い方まとめ

| 用途 | 推奨ボタン |
|------|-----------|
| 通常のゲーム操作（A ボタン） | POWER / GPIO18 |
| 書き込みモード移行 | BOOT |
| 電源 ON/OFF | POWER |
| 長押しゲーム入力（バッテリー使用時） | 要注意・長押し閾値対応が必要 |
