# 作業スクラッチ

調査中の問題・仮説・試みた対策を記録する一時ファイル。
解決したら重要な気づきを PROGRESS.md に転記してセクションを削除すること。

---

## [解決済み] minigb_apu 音質改善

### 発端・目的

- BGM 中の金属音っぽいところに違和感。音量も大きく前面に出すぎている印象
- コイン取得 SE に「追いついていない」感じ（タイミングのもたつき）
- 元の minigb_apu 実装を精査したところ **実装ミス・未実装** が複数確認された
- まずローカルで修正を試み、うまくいけば元リポジトリへ Issue / PR を送ることも検討

### 根本原因の分析（2026-06-04 時点）

**問題 A：フレームシーケンサーが未実装（→コイン音のタイミング崩れ）**

- 元実装は length / envelope / sweep を「サンプルごとに連続更新」
- 本来は 512 Hz の離散クロック（Pan Docs のステップ 0〜7）
  - Step 0,2,4,6: Length（256 Hz）
  - Step 2,6: Sweep（128 Hz）
  - Step 7: Envelope（64 Hz）
- 短い SE（コイン等）はこの離散性のズレが相対的に大きく出る

**問題 B：高周波矩形波のエイリアシング（→BGM の金属音・音量増大）**

- CH1/CH2 の高音域（GB freq 1800〜2000 台）で実周波数が 7 kHz 超
- 32 kHz サンプリング、ナイキスト 16 kHz を超えた倍音が折り返して聴こえる
- 折り返した倍音が知覚音量を上げ「前面に出る」印象を作る

### 試行ログ

| # | 内容 | 結果 |
|---|------|------|
| 1 | フレームシーケンサーを実装。コールバックでバッファ全体先行処理→後でシーケンサー進行 | コイン音が完全消失（length counter がバッファ生成前に尽きる） |
| 2 | 生成 → シーケンサー進行の順に変更（1 バッファ分の遅延を許容） | コイン音が一部復活。ただし戦闘音・場面転換 SE が途切れる |
| 3 | チャンクインターリーブ（64 samples / chunk = 1 tick 分）。`update_square/wave/noise` に offset/count を追加 | BGM の金属音が戻る（アプリ側への知覚に悪影響）。SE も変に削られる。**全体として劣化** |

### 現状（2026-06-04）

試行 3 の実装がコミット済みだが **劣化** と判定。原因の切り分けができていない。

次の仮説と対策候補：

1. **チャンクサイズの問題？** 64 サンプル（約 2 ms）では tick_frame_sequencer が粗すぎる or 細かすぎる
2. **tick_len の挙動が間違っている？** enabled を落とすタイミングがまだずれている可能性
3. **エイリアシングを先に対処すべき？** ローパスフィルタを入れて金属音だけ緩和し、SE タイミングは現状維持という方針も

### 試行 4（結果：不十分）：チャンク廃止 + 一次 LPF

金属音の改善不十分・SE カットは継続。フレームシーケンサーが SE カットの主因と判断。

### 試行 5（完了・コミット済み 82c9e8b）：バグ修正のみ

コイン音が大幅改善。BGM の金属音は「カンカン」→「キンキン」に改善、残差はエイリアシング。

### 試行 6（リバート済み）：PolyBLEP — 効果なし・コイン音悪化

CH1/CH2 にのみ効くが、金属音に変化なし。コイン音はむしろ悪化。
→ **金属音の原因は CH3（波形メモリ）または CH4 の可能性が高い。**

PolyBLEP は CH1/CH2 の矩形波エイリアシングを抑える技術なので、
音源チャンネルが違えば効果なしは当然の結果。

### チャンネル特定結果

| チャンネル | 消音時の変化 |
|-----------|------------|
| CH1 | 変化なし |
| CH2 | 変化なし |
| CH3 | 変化なし |
| CH4 | 金属音・コイン音ともに消える → **CH4 が原因** |

コイン音も CH4 だったため、LFSR 修正（Bug 1）はコイン音の改善に直接寄与していた（筋が通っている）。

### 試行 7（未実施・方針変更）：CH4 出力への一次 LPF

LPF 案は検討したが、**CH4 出力を 25%（>> 2）スケールダウン**するだけで十分と判断し不採用。
→ コミット `10f0dc5` で対応済み。LPF・フレームシーケンサー改善は今後の課題として保留。

---


## [解決済み] AMOLED メニューフリーズバグ

### 原因（確定・2026-06-05）

FT3168（タッチ）と ES8311（音声コーデック）が同一 I2C バス（i2c1）を共有。
両ドライバの I2C 呼び出しが全てタイムアウトなし（`i2c_write_blocking` / `i2c_read_blocking`）。

### 修正済み（コミット 9d64934）

- ES8311 ドライバの `wr()` / `rd()` → タイムアウト付き版に変更（5ms）
- FT3168 ドライバの `write_byte()` / `read_byte()` / `read_bytes()` → タイムアウト付き版に変更（3ms）
- 結果：**音量変更時のフリーズは解消**

### 残存問題（2026-06-05）

**SD バックアップを押すとフリーズする**問題が継続。

#### 原因（確定・2026-06-05）

シリアル出力で **Out of memory** を確認。FatFS のロングファイルネーム（LFN）対応が `malloc` でバッファを確保しようとするが、ゲーム起動後はヒープが枯渇しており失敗する。

- ゲーム起動前（ROM ロード・SD コピー時）はメモリに余裕がありため成功する
- ゲーム起動後はフレームバッファ・音声バッファ・リングバッファ等でヒープがほぼ満杯

#### 修正（実装済み・実機確認済み）

`ffconf.h` の `FF_USE_LFN 3`（malloc）→ `FF_USE_LFN 1`（静的バッファ）に変更。

- 全ファイルパスが 8.3 互換名（`kaeru.sav`, `kaeru.gb` 等）なので LFN 機能自体は不要だが、
  静的バッファにすれば malloc 不要・LFN は保持できる
- SD 操作は Core 0 からのみ（非同時アクセス）→ `FF_USE_LFN 1` のスレッド非安全は問題なし

---
